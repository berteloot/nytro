services:
  - type: web
    name: nytro-dashboard
    runtime: node
    buildCommand: yarn
    startCommand: npm install --include=dev && npm run build
    envVars:
      - key: NODE_VERSION
        value: 22.16.0

